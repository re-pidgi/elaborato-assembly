



leggi_file
    syscallleggifile ##########

    array len 4field * 10prod * 1byte

    read_integer dentro ad array
    aumenta array di 1

    se EOF restituisci il valore di prodotti e l'array
    altrimenti skippa le virgole e whitespace e leggi di nuovo


read_integer
    leggi 1 carattere da file pointer
    se carattere è numero moltiplica ACC per 10 e aggiungi il numero
    restituisci ACC

menu
    output prompt scelta algoritmo
    input carattere
    1 o 2 restituisci 0 o 1
    altro domanda di nuovo

order
    restituisci un array dell'ordine scelto

main
    call leggi_file
    call menu
    call order

    per ogni broski nell'array ordinato
        aumenta il contatore per il tempo voluto
        controlla il tempo rispetto la scadenza
        aggiungi la penale se c'è

    ripeti da call menu